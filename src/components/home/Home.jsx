import {Navigation} from "./Navigation";

export const Home = () => {
    return <section className="home">
            <Navigation/>
            <div className="home__rules">
                    <h1 className="home__rules--title">Rules</h1>
            </div>
    </section>
}